package com.example.ShortenerLink.repository;

import com.example.ShortenerLink.domain.AllStatistics;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AllStatisticsRepository extends JpaRepository<AllStatistics, Long> {
}
